export default ({ app, query, store, redirect }) => {
    /*
    console.log("backend-action middleware")
    console.log("query", query.act, query.msg)
    return;
	if (query.act == "activation") {
		if (query.msg == "invalid_link") {
			console.log(app.$t('error_activate_set'));
			redirect({name: 'login'});
		} else if (query.msg == "server_error") {
			console.log(app.$t('error_server'));
			redirect({name: 'login'});
		} else if (query.msg == "success") {
			console.log(app.$t('success_activate_set'))
			store.dispatch('auth/saveToken', query.token);
			redirect({name: 'profile'});
		}
    }
    */

  }
  